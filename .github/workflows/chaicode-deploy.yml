# name: Deploy NodeJS on Local VM

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Deploy on Local VM
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.HOST }}
#           username: megha
#           key: ${{ secrets.PRIVATE_KEY }}
#           script: |
#             cd /home/megha/node-server-devops
#             git pull origin main
#             npm install
#             pm2 restart node-server || pm2 start index.js --name node-server

name: Deploy NodeJS on Local VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    
    steps:
      - name: Deploy Application

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: QEMU
        uses: docker/setup-qemu-action@v4
        name: set up Docker Buildx
        uses: docker/setup-buildx-action@v3

        -name: Docker Build and Push
        run: |
          docker build -t abcd56/node-server .
          docker push abcd56/node-server

        run: |
          cd /home/megha/node-server-devops
          git pull origin main
          npm install
          pm2 restart node-server || pm2 start index.js --name node-server